# Normalize line endings: store text as LF in the repo, check out native on Windows.
* text=auto

# Keep these as LF everywhere (Go tooling and web assets prefer it).
*.go   text eol=lf
*.mod  text eol=lf
*.html text eol=lf
*.txt  text eol=lf
